output:
  ok: "ok"
  done: "done."
  warn: "WARN"
  error: "ERROR"
  detail_prefix: "·"
  confirm_continue: "Do you want to continue? [y/N]:"
  ok_all: "Everything is ok!"
  downloaded_only: "Downloaded (not installed)."
  field_identity: "Identity:"
  field_name: "Name:"
  field_bucket: "Bucket:"
  field_description: "Description:"
  field_version: "Version:"
  field_homepage: "Homepage:"
  field_license: "License:"
  field_shims: "Shims:"
  select_prompt: "Please select one, enter the number to continue:"
  select_one: "Select one (0-%{max}):"
status:
  resolving: "Resolving packages..."
  sizing: "Calculating download size..."
  downloading: "Downloading packages..."
  checking_hash: "Checking hash..."
  resolving_done: "Resolving packages completed."
  sizing_done: "Download size calculated."
  downloading_done: "Download completed."
detail:
  extract_done: "extract done"
  shim_done: "shim done"
  shim_removed: "shim removed"
  shortcut_done: "shortcut done"
  shortcut_removed: "shortcut removed"
  persist_done: "persist done"
  adding_env_path: "adding env path..."
  env_path_added: "env path added"
  setting_env_var: "setting env var..."
  env_var_set: "env var set"
  removing_env_path: "removing env path..."
  env_path_removed: "env path removed"
  removing_env_var: "removing env var..."
  env_var_removed: "env var removed"
  removing_persist: "removing persisted data..."
  psmodule_removed: "psmodule removed"
  extracting: "extracting: %{ctx}"
  extracting_done: "extract done"
  checking_hash_item: "Checking hash...%{ctx}"
  creating_shim: "creating shim: %{ctx}"
  creating_shortcut: "creating shortcut: %{ctx}"
  removing_shim: "removing shim: %{ctx}"
  removing_shortcut: "removing shortcut: %{ctx}"
  removing_psmodule: "removing psmodule: %{ctx}"
  loading_cache: "Loading %{filename} from cache."
  unlinking: "Unlinking %{path}"
  linking: "Linking %{from} => %{to}"
  shortcut_conflict: "shortcut already exists and belongs to another package; it will be overwritten: %{path}"
  shortcut_not_found: "shortcut not found: %{path}"
  shim_conflict: "shim already exists and belongs to another package; it will be overwritten: %{path}"
  config_fallback: "all config paths failed, falling back to default config"
reinstall:
  released: "Packages released."
  reheld: "Packages re-held."
cmd:
  header_aliases: "Aliases"
  header_buckets: "Updating buckets"
  header_buckets_list: "Known buckets"
  header_installed_apps: "Installed apps"
  list_header_name: "Name"
  list_header_version: "Version"
  list_header_source: "Source"
  list_header_info: "Info"
  list_summary: "%{count} installed, %{held} held, %{upgradable} upgradable"
  no_aliases: "No aliases configured."
  alias_add_usage: "Usage: hok alias add <name> <command>"
  alias_added: "alias added: %{name} -> %{value}"
  alias_rm_usage: "Usage: hok alias rm <name>"
  alias_removed: "alias removed: %{name}"
  alias_unknown: "Unknown command: '%{cmd}'. Use: list, add, rm"
  no_apps_found: "No apps directory found."
  no_current_symlink: "no 'current' symlink"
  missing_install_json: "missing install.json"
  missing_manifest_json: "missing manifest.json"
  no_issues: "No issues found."
  run_reset: "run 'hok reset <app>' to fix"
  bucket_err: "Err"
  cache_cleaned: "Cache cleaned."
  cache_all_removed: "All download caches were removed."
  cache_matching_removed: "All caches matching '%{query}' were removed."
  cat_not_found: "Could not find package named '%{query}'."
  cat_invalid: "Invalid input."
  checkhashes_skipped: "skipped (no hash)"
  checkhashes_rehashed: "re-hashed"
  checkhashes_mismatch_upd: "hash mismatch! updated"
  checkhashes_mismatch: "hash mismatch expected %{expected} got %{actual}"
  checkhashes_summary: "Scanned %{total}: %{passed} ok, %{failed} failed, %{updated} updated."
  checkver_updated_to: "updated to %{ver}"
  checkver_update_failed: "update failed: %{e}"
  checkver_no_version: "script returned no version"
  checkver_script_err: "script error: %{e}"
  checkver_github_err: "could not extract GitHub repo from homepage"
  checkver_sourceforge_err: "could not extract SourceForge project"
  checkver_no_url: "no checkver url"
  checkver_no_extract: "could not extract version"
  checkver_version_wildcard: "cannot use --version with wildcard app pattern"
  checkurls_summary: "Checked %{total} URLs: %{valid} valid, %{invalid} invalid."
  checkurls_no_manifests: "No manifests to check."
  cleanup_no_old: "No old versions to clean up."
  cleanup_removed: "%{count} old version(s) removed"
  cleanup_removed_failed: "%{count} old version(s) removed, %{failed} failed (retry after restart)"
  config_set: "Config '%{key}' has been set to '%{value}'"
  config_unset: "Config '%{key}' has been unset"
  create_url_required: "URL is required."
  create_manifest_saved: "Manifest saved to: %{path}"
  depends_invalid: "Invalid selection."
  depends_not_found: "Could not find package named '%{query}'."
  dir_not_found: "error: '%{path}' is not a directory"
  err_download: "download failed: %{e}"
  err_hash: "hash error: %{e}"
  formatjson_none: "No manifests needed formatting."
  formatjson_count: "Formatted %{count} manifest(s)."
  hold_err: "Err"
  already_held: "'%{name}' is already held."
  not_held: "'%{name}' is not held."
  hold_failed: "Failed to hold '%{name}'."
  hold_not_installed: "'%{name}' is not installed."
  hold_not_installed_global: "'%{name}' is not installed globally."
  import_complete: "Import complete."
  import_no_pkgs: "No packages found in import file."
  install_ok: "All apps are up to date."
  checkup_ok: "Everything is shiny now!"
  no_apps: "No apps installed."
  no_old_versions: "No old versions to clean up."
  no_pkgs_found: "No packages found."
  outdated: "All apps are up to date."
  prefix_installed: "Package '%{pkg}' is installed but has no 'current' symlink."
  prefix_no_pkg: "Package '%{pkg}' is not installed."
  refresh_cache: "Refreshing manifest cache..."
  cache_done: "Manifest cache refreshed."
  shim_info_usage: "Usage: hok shim info <name>"
  shim_no_dir: "No shims directory found."
  missing_checkver_scan: "Scanned %{total} manifests: %{missing} missing checkver, %{noauto} missing autoupdate."
  missing_checkver_all: "All manifests have checkver and autoupdate."
  home_not_found: "Could not find package named '%{query}'."
  home_multiple: "Found multiple packages named '%{query}':"
  home_invalid: "Invalid input."
  header_buckets: "Updating buckets"
  header_installed: "The following packages will be INSTALLED:"
  header_upgraded: "The following packages will be UPGRADED:"
  header_replaced: "The following packages will be REPLACED:"
  header_removed: "The following packages will be REMOVED:"
  vt_skip: "skipped (set VT_API_KEY to scan)"
  holding: "Holding"
  unholding: "Unholding"
  unhold_failed: "Failed to unhold '%{name}'."
  adding_bucket: "Adding bucket"
  removing_bucket: "Removing bucket"
  downloading: "Downloading"
  computing_hash: "Computing hash"
  found: "Found %{count} package(s) for query '%{query}'"
  no_download_urls: "no download URLs"
  held_skip: "'%{name}' is held to version %{version}"
  reinstall_not_installed: "'%{name}' is not installed. Use 'hok install' to install it."
  admin_rights_required: "ERROR: you need admin rights to %{verb} global apps"
  update_global_no_app: "hok update: --global is invalid when <app> is not specified."
  auto_err_token: "GITHUB_TOKEN is required for ci-auto-pr"
  auto_err_mode: "Must specify --push or --request"
  auto_err_upstream: "--request requires --upstream (<user>/<repo>:<branch>)"
  auto_pr_header: "ci-auto-pr"
  auto_pr_no_changes: "No manifests updated."
cli:
  config_set: "Config '%{key}' has been set to '%{value}'"
  config_unset: "Config '%{key}' has been unset"
error:
  bucket_not_found: "bucket '%{name}' does not exist"
  bucket_already_exists: "bucket '%{name}' already exists"
  bucket_add_remote_required: "'%{name}' is not a known bucket, <repo> is required"
  bucket_update_not_fast_forward: "Cannot fast-forward update of bucket '%{ref}': histories diverged or unrelated (remote may have been rewritten)"
  bucket_update_merge_conflict: "Merge conflict while updating bucket '%{ref}'; resolve the conflicts in the bucket directory and retry"
  bucket_update_local_changes: "Local changes would be overwritten by the bucket update: %{files}"
  config_in_use: "Could not alter config because it is in use."
  config_key_invalid: "invalid config key '%{key}'"
  config_value_invalid: "invalid config value '%{value}'"
  user_agent_already_set: "User agent already set"
  output_already_set: "Output sink already set"
  hash_mismatch: "Hash mismatch"
  invalid_cache_file: "Invalid cache file"
  invalid_answer: "invalid answer"
  package_not_found: "Could not find package named '%{name}'"
  package_cascade_remove_hold: "Trying to cascade uninstall held package '%{name}'"
  package_dependent_found: "Found dependent(s)"
  package_multiple_candidates: "Found multiple candidates for package named '%{name}'"
  package_hold_not_installed: "package '%{name}' is not installed"
  package_hold_broken_install: "package '%{name}' is broken"
  app_running: "'%{name}' is still running! Close the app(s) before proceeding."
  custom: "%{msg}"
  extraction_failed: "failed to extract archive: %{reason}"
  path_traversal_detected: "path traversal detected: %{detail}"
  cyclic_dependency: "Cyclic dependency detected"
  hash: "Hash error"
  git: "Git error"
  io: "I/O error"
  regex: "Regular expression error"
  sqlite: "SQLite error"
  serde: "Serialization error"

cli_help:
  heading:
    usage: "Usage:"
    subcommands: "Subcommands"
    options: "Options"
  after_help: "Type 'hok help <command>' to get help for a specific command."
  help:
    about: "Print this message or the help of the given subcommand(s)"
  hok:
    about: "Hok is a CLI implementation of Scoop in Rust"
    args:
      detail: "Show detailed operation information for debugging"
      language: "Force CLI language (auto/en/zh)"
      no_color: "Disable colored output"
      verbose: "Increase logging verbosity"
      quiet: "Decrease logging verbosity"
      help: "Print help"
      version: "Print version"
  alias:
    args:
      command: "Command: list (default), add, rm"
      name: Alias name (for add/rm)
      value: Alias command (for add)
    about: "List, add, or remove Scoop aliases"
    long_about: "List, add, or remove Scoop aliases\n\nExamples:\n  hok alias list                   list all aliases\n  hok alias add <name> <command>   add an alias\n  hok alias rm  <name>            remove an alias"
  bucket:
    about: Manage manifest buckets
    args:
      add: Add a bucket
      list: List buckets
      list.known: List known buckets
      known: "List known buckets (Scoop: scoop bucket known)"
      remove: Remove bucket(s)
      remove.name: The bucket name(s)
  cache:
    about: Package cache management
    args:
      list: List download caches
      remove: Remove download caches
      remove.all: Remove all caches
  cat:
    args:
      package: Name of the package to be inspected
    about: Inspect the manifest of a package
  checkhashes:
    about: Verify and update manifest hashes
    args:
      app: "Specific app(s) to check (supports wildcards, default: all)"
      cache: Use cache directory for downloaded files
      dir: Bucket directory to scan for manifests
      force: Force update manifest even when hash matches (re-hash)
      update: Update manifest with correct hash when mismatch found
  checkup:
    about: Check for potential problems with installed packages
  checkurls:
    about: Check manifest URLs for validity
    args:
      app: "Specific app(s) to check (supports wildcards, default: all)"
      dir: Bucket directory to scan for manifests
      skip_valid: Only show invalid URLs (suppress valid ones)
      timeout: Request timeout in seconds
  checkver:
    about: Check manifest for a newer version
    args:
      app: "Specific app(s) to check (supports wildcards, default: all)"
      dir: Bucket directory to scan for manifests
      update: Update manifest with new version and trigger autoupdate
      force-update: Force update even when version is unchanged (useful for hash updates)
      skip-updated: Skip manifests that are already up-to-date
      version: Update manifest to specific version (skip version detection)
      timeout: Request timeout in seconds
  cleanup:
    about: Cleanup apps by removing old versions
    args:
      app: Given named app(s) to be cleaned up (all apps if empty)
      all: Cleanup all apps (alternative to '*')
      global: Cleanup a globally installed app
      cache: Remove download cache simultaneously
  completions:
    args:
      shell: The shell type
    about: Generate shell completions
  config:
    about: Configuration management
    long_about: |-
      Configuration management

      Config file: ~/.config/hok/config.json

      Settings
      --------

      aria2-enabled: bool, default true
            Enable fragmented downloads (hok's built-in HTTP range splitter).
      aria2-split: int, default 5
            Number of connections used for each fragmented download.
      aria2-max-connection-per-server: int, default 5
            Maximum number of connections to one server per download.
      aria2-min-split-size: size, default 5M
            Minimum file size to trigger fragmented downloads (e.g. 5M, 10M).
      cache_path: path, default $SCOOP_CACHE or <root>/cache
            Download cache directory.
      cat_style: string, default none
            When set, use 'bat' to display manifests (requires bat installed).
      default_architecture: 64bit|32bit|arm64, default auto-detected
            Preferred architecture used for installation.
      global_path: path, default $SCOOP_GLOBAL or %ProgramData%\scoop
            Root directory for globally installed apps.
      gh_token: string, default none
            GitHub API token used for authenticated requests.
      ignore-failures: bool, default true
            Continue multi-package operations despite individual failures.
      ignore_running_processes: bool, default false
            Proceed even if the app is running (warning instead of abort).
      language: auto|en|zh, default auto
            CLI language for messages and help text.
      last_update: string, internal
            Timestamp of the last bucket update (managed by hok).
      no-color: bool, default false
            Disable colored output.
      no_junction: bool, default false
            Do not use the 'current' junction; shims point to version dirs instead.
      output-style: scoop|pacman, default scoop
            Output style of progress and status messages.
      private_hosts: array, edit file
            Private hosts needing extra auth headers; edit the config file directly.
      proxy: string, default none
            Proxy settings: none | default | currentuser | [username:password@]host:port.
      root_path: path, default $SCOOP or ~/scoop
            Scoop root directory.
      use_isolated_path: bool|string, default none
            Store apps' PATH entries in a dedicated env var (SCOOP_PATH by default).
      use_sqlite_cache: bool, default false
            Use SQLite database for manifest caching (Scoop-compatible schema).
      virustotal_api_key: string, default none
            VirusTotal API key used for uploading/scanning files.

      Most settings can be changed with 'hok config set <key> <value>' (empty
      value unsets). private_hosts and last_update are only editable in the
      file. Run 'hok config list --all' to see current values and effective
      defaults.
    args:
      edit: "Edit the config file [default: system default editor]"
      list: "List all settings in key-value (--all also shows defaults)"
      list.all: Also show the effective default value of every setting
      set: Add a new setting to the config file
      unset: Remove a setting from config file
  create:
    about: "Create a manifest from a download URL"
    args:
      url: Download URL
      output: "Optional output file (default: stdout)"
  depends:
    args:
      package: Name of the package
    about: Show dependencies of a package
  download:
    about: Download apps in the cache folder and verify hashes
    long_about: |-
      Download app files into the cache directory without installing them
      (uses your local 'buckets'):
           hok download git

      To download a different version of the app
      (note that this will auto-generate the manifest using the current version):
           hok download gh@2.7.0

      To download an app from a manifest at a URL:
           hok download https://raw.githubusercontent.com/ScoopInstaller/Main/master/bucket/runat.json

      To download an app from a manifest on your computer:
           hok download \path\to\app.json
    args:
      package: The package(s) to download (name, bucket/name, name@version, or manifest URL/path)
      force: Force download (overwrite cache)
      no_hash_check: Skip package integrity check (use with caution!)
      arch: "Use the specified architecture (32bit/64bit/arm64), overriding the detected default"
  export:
    about: Export installed packages list
    args:
      all: Include non-bucket packages (URL/path installs)
  formatjson:
    about: Format manifest JSON files in a bucket directory
    args:
      app: "Specific app(s) to format (supports wildcards, default: all)"
      dir: Bucket directory to scan for manifests
  hold:
    about: Hold package(s) to disable changes
    args:
      package: The package(s) to be held
  home:
    args:
      package: The package name
    about: Browse the homepage of a package
  import:
    args:
      file: JSON file to import from
    about: Import installed packages from a file
  info:
    args:
      query: The query string (regex supported)
    about: Show package(s) basic information
  install:
    about: Install package(s)
    long_about: |-
      Install package(s) from your local buckets, a manifest URL, or a local
      manifest file.

      The usual way to install an app (uses your local 'buckets'):
           hok install git

      To install a different version of the app
      (note that this will auto-generate the manifest using the current version):
           hok install gh@2.7.0

      To install an app from a manifest at a URL:
           hok install https://raw.githubusercontent.com/ScoopInstaller/Main/master/bucket/runat.json

      To install a different version of the app from a URL:
           hok install https://raw.githubusercontent.com/ScoopInstaller/Main/master/bucket/neovim.json@0.9.0

      To install an app from a manifest on your computer:
           hok install \path\to\app.json
           hok install \path\to\app.json@version
    args:
      arch: "Use the specified architecture (32bit/64bit/arm64), overriding the detected default"
      assume_yes: Assume yes to all prompts and run non-interactively
      download_only: Download package(s) without performing installation
      escape_hold: Escape hold to allow changes on held package(s)
      global: Install globally (to $SCOOP_GLOBAL)
      ignore_cache: Ignore cache and force download
      ignore_failure: Ignore failures to ensure a complete transaction
      independent: Do not install dependencies (may break packages)
      no_hash_check: Skip package integrity check
      no_replace: Do not replace package(s)
      no_upgrade: Do not upgrade package(s)
      offline: Leverage cache and suppress network access
      package: "The package(s) to install (name, bucket/name, name@version, or a manifest URL/path)"
  list:
    about: List installed package(s)
    args:
      explicit: Turn regex off and use explicit matching
      held: List held package(s)
      query: The query string (regex supported by default)
      upgradable: List upgradable package(s)
      versions: Show all installed versions (not just current)
  missing_checkver:
    about: Check bucket manifests missing checkver and autoupdate
    args:
      dir: Bucket directory to scan for manifests
      supported: Only show manifests that have checkver/autoupdate (inverse)
  prefix:
    args:
      package: Name of the package
    about: Show the directory where a package is installed
  reinstall:
    about: Reinstall a package
    args:
      ignore_cache: Ignore cache and force download
      ignore_failure: Ignore failures to ensure a complete transaction
      no_hash_check: Skip package integrity check
      offline: Leverage cache and suppress network access
      package: The package(s) to reinstall
  reset:
    args:
      app: The app name
      version: A specific version to reset to
    about: "Reset an app to resolve conflicts (reapply shims, shortcuts, post_install)"
  search:
    about: "Search available package(s)"
    args:
      explicit: Turn regex off and use explicit matching
      query: The query string (regex supported by default)
      with_binary: Search through package binaries as well
      with_description: Search through package descriptions as well
  shim:
    about: List or inspect shims
    args:
      command: "Command: info (default), list"
      name: Shim name (for info command)
  status:
    about: Show the status of all installed apps
    args:
      all: "Also show up-to-date apps (default: only outdated)"
  unhold:
    about: Unhold package(s) to enable changes
    args:
      package: The package(s) to be unheld
  uninstall:
    about: Uninstall package(s)
    args:
      escape_hold: Escape hold to allow changes on held package(s)
      ignore_failure: Ignore failures to ensure a complete transaction
      no_hash_check: Skip package integrity check
      package: The package(s) to uninstall
  update:
    about: "Fetch and update subscribed buckets, or upgrade installed package(s)"
    long_about: "Fetch and update subscribed buckets, or upgrade installed package(s)\n\nExamples:\n  hok update         update buckets only\n  hok update <app>   upgrade a specific package (Scoop-compatible)\n  hok update *       upgrade all packages"
    args:
      assume_yes: Assume yes to all prompts and run non-interactively
      escape_hold: Escape hold to allow to upgrade held package(s)
      force: "Force update: bypass the cooldown and reinstall apps already at the requested version"
      ignore_cache: Ignore cache and force download
      ignore_failure: Ignore failures to ensure a complete transaction
      independent: Do not install dependencies (may break packages)
      no_hash_check: Skip package integrity check
      no_replace: Do not replace package(s)
      no_upgrade: Do not upgrade package(s)
      offline: Leverage cache and suppress network access
      package: The package(s) to be upgraded (omit to only update buckets)
      global: Install globally (to $SCOOP_GLOBAL)
      all: Update all installed packages (alternative to '*')
  upgrade:
    about: Upgrade installed package(s)
    args:
      assume_yes: Assume yes to all prompts and run non-interactively
      escape_hold: Escape hold to allow to upgrade held package(s)
      ignore_failure: Ignore failures to ensure a complete transaction
      no_hash_check: Skip package integrity check
      offline: Leverage cache and suppress network access
      package: "The package(s) to be upgraded (default: all except held)"
  virustotal:
    about: "Check a package's download URL against VirusTotal"
    args:
      app: Package name(s) to check
  which:
    about: Show the shim location(s) of a command
  ciautopr:
    about: "Auto-update manifests and create pull-requests via GitHub API (zero external binary, CI mode)"
    args:
      dir: Bucket directory to scan for manifests
      upstream: "Upstream repository with target branch (<user>/<repo>:<branch>)"
      push: Push updates directly to origin branch
      request: Create pull-requests for each update
      origin_branch: Origin (local) branch name
      message: "Commit message format (<app> and <version> are replaced)"
      skip_updated: Skip manifests that are already up-to-date
      special: Force update on these special snowflakes (comma-separated)
      token: "GitHub Token (default from GITHUB_TOKEN env var)"

# Command aliases (for shell completions)
cli_alias:
  bucket:
    List: [ls]
    Remove: [rm]
  cache:
    List: [ls]
    Remove: [rm]
  config:
    List: [ls]
  search: [s]
  uninstall: [rm, remove]
  update: [u]
